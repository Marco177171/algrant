chat:
  # visualizzazione: bottom-up con ultimo messaggio in fondo
  # ultime-chat: ordina le chat in base al messaggio non visto più recente, visualizza amount di nuovi messaggi
  message_counter-context_processor: count_my_unseen_message. If any, show_amount_on_each_page
  # create_new_chat_with_single_user (message button on user profile): maybe better as it is
  # filter on chats
  # erase_chat_if_creator:
  # add_user_to_existing_chat:
  # admin_dashboard: url + view -> add_user_to_chat, delete_chat, remove_user_from_chat
  # participant_dashboard: url + view -> leave_chat

comments:
  # delete_my_comment:

parametrizzazione:
  minimizzazione della ridondanza: js in different files from htmls
  context_processors and filters: use params as much as you can

push_notifications:
  funzione: far squillare i telefoni
  evitare duplicati: prima di attivare una nuova sottoscrizione, controllare che lo user non ne abbia già 
  # error_handler: get to create notification subscription in db when user grants permission
  # activation notification: send dummy notification if user activates successfully

Password management:
  # accettare ogni tipo di password: eliminare restrizioni
  